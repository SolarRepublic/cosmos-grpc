// root tsconfig extended by others; this should never be used by `tsc`
{
	"extends": "@blake.regalia/tsconfig/tsconfig.node.json",

	"compilerOptions": {
		"baseUrl": ".",

		"declaration": true,
		"noImplicitReturns": false,

		"allowJs": false,

		"module": "ESNext",
		"moduleResolution": "node10"
	},

	"include": [
		"src/**/*.ts",
	],

	"ts-node": {
		"experimentalSpecifierResolution": "node"
	},
}
