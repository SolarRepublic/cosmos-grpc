// for linting and compiling typescript from build/lib → build/dist
{
	"extends": "./tsconfig.json",

	"compilerOptions": {
		"baseUrl": "build/lib",
		"outDir": "build/dist",
		"declarationDir": "build/dist",

		"paths": {
			"#/*": ["./*"]
		}
	},

	"include": [
		"build/lib/**/*.ts",
	],
}